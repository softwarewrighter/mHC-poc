# CUDA phase (planned)

This directory is a placeholder for Phase 2:
- HF Transformers + PyTorch reference implementation of the same mHC residual logic.
- FlashAttention-2 optional.
- Unsloth optional **only if** it remains compatible with forward-pass modifications.

The MLX version is the source of truth for correctness.
